{# poems index display page #}

{% extends 'TestPublicationsBundle::layout.html.twig' %}

{% block body -%}

<h1>Poems</h1>

    <div class="container">

        {% for entity in entities %}
            <ul>
                <div class="span12">
<div class="span1">
                <a href="{{ entity.url }}"><i class="icon-hand-right"></i></a>
</div>
<div class="span11">
		<h4>{{ entity.title }}</h4>
</div>
                <a href="{{ entity.url }}"><p>{{ entity.journal }}, {{ entity.pubyear }}</p></a>
		</div>
		</ul>
<div class="row">
<div class="span12">
                <ul>
                    <li>
                        <a href="{{ path('poems_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('poems_edit', { 'id': entity.id }) }}"><i class="icon-edit"></i></a>
                    </li>
                </ul>
              </div>
</div>
        {% endfor %}
</div>

        <ul>
        <li>
            <a href="{{ path('poems_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
